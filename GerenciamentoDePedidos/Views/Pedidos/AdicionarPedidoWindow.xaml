<Window x:Class="GerenciamentoDePedidos.Views.Pedidos.AdicionarPedidoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adicionar Pedido" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Campos -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Botões -->
        </Grid.RowDefinitions>

        <!-- Seção de Entrada -->
        <GroupBox Header="Informações do Pedido" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Cliente -->
                <Label Content="Cliente:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="cbCliente" Grid.Column="1" Margin="0,0,15,0"/>

                <!-- Produto -->
                <Label Content="Produto:" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="cbProduto" Grid.Column="3" Margin="0,0,15,0"/>

                <!-- Quantidade -->
                <Label Content="Qtd:" Grid.Column="4" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="txtQuantidade" Grid.Column="5" Margin="0,0,15,0"/>

                <!-- Botão Adicionar -->
                <Button Content="Adicionar" Grid.Column="6" 
                        Background="#2196F3" Foreground="White" 
                        Padding="5,2" Width="100"
                        Click="BtnAdicionarProduto_Click"/>
            </Grid>
        </GroupBox>

        <!-- DataGrid -->
        <DataGrid x:Name="dgProdutos" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True"
                  AlternatingRowBackground="#F5F5F5"
                  Margin="0,0,0,15" HeadersVisibility="Column" 
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produto" Binding="{Binding NomeProduto}" Width="2*"/>
                <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="1*"/>
                <DataGridTemplateColumn Header="Ações" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remover"
                                    Background="#F44336" Foreground="White"
                                    Padding="4,2" Margin="5,0"
                                    HorizontalAlignment="Center"
                                    Click="BtnRemoverProduto_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Rodapé -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Total -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Total: " FontSize="16" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock x:Name="txtValorTotal" Text="R$ 0,00" FontSize="16" FontWeight="Bold" Foreground="#4CAF50"/>
            </StackPanel>

            <!-- Botões -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Salvar Pedido" Width="130"
                Background="#4CAF50" Foreground="White"
                FontWeight="Bold" Padding="6,3" Margin="0,0,10,0"
                Click="BtnSalvarPedido_Click"/>
                <Button Content="Cancelar" Width="100"
                Background="#9E9E9E" Foreground="White"
                Padding="6,3" Click="BtnCancelar_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
    
    
